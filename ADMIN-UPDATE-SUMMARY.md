# สรุปการอัปเดต: โครงสร้างชีตใหม่ + หน้า Admin

_บันทึกเมื่อ 12 สิงหาคม 2026_

## 1. ปรับโครงสร้างชีตให้เป็น "1 ผู้สมัคร 1 แถว"

Deploy เป็น Version 13 เรียบร้อยแล้ว และทดสอบส่งใบสมัครจริงผ่าน backend ที่ deploy อยู่ (ไม่ใช่แค่ทดสอบในเครื่อง) โดยจงใจกรอกข้อมูลเกินจำนวนที่เผื่อไว้ทุกหมวด (วุฒิการศึกษา 5 รายการ, ประวัติงาน 7 รายการ, ผู้ติดต่อฉุกเฉิน 5 คน, ภาษา/โปรแกรมเพิ่มเติมอย่างละ 5, ไฟล์แนบ 7 ไฟล์) แล้วเข้าไปเปิดดู Google Sheet จริงเพื่อตรวจสอบ ผลคือ:

- ช่องที่เผื่อไว้ (`Education1_*` ถึง `Education3_*`, `WorkHistory1_*` ถึง `WorkHistory5_*` ฯลฯ) กรอกข้อมูลถูกต้องครบ
- รายการที่เกินจำนวนที่เผื่อไว้ ถูกรวมไปไว้ในคอลัมน์ `...Extra` ท้ายสุดถูกต้อง — **ไม่มีข้อมูลหายไปเลย**
- ไฟล์แนบที่อัปโหลดไปสร้างจริงใน Drive และลิงก์ถูกบันทึกไว้ในแถวถูกต้อง (`PhotoURL`, `CVURL`, `AdditionalAttachment1_URL`..`3_URL`)
- แท็บเก่า (`Education`, `WorkHistory`, `EmergencyContacts`, `AdditionalLanguages`, `AdditionalComputerApps`, `Attachments`) **ไม่มีแถวใหม่เพิ่มเข้ามาเลย** — ยืนยันว่าเลิกเขียนไปที่แท็บเก่าจริง (แท็บเหล่านี้ยังอยู่ มีแต่ข้อมูลทดสอบเก่า ลบเองได้ถ้าต้องการ)
- ระบบเดิม (ดรอปดาวน์เลือกตำแหน่งงานในฟอร์มสาธารณะ, `?action=positions`) ยังทำงานปกติ ไม่กระทบ
- ลบแถวทดสอบทิ้งให้เรียบร้อยแล้วหลังตรวจสอบ

## 2. หน้า Admin (`admin.html`)

โค้ด deploy ขึ้นแล้ว แต่ **ยังใช้งานจริงไม่ได้** จนกว่าจะทำขั้นตอนนี้เอง (Claude ทำแทนไม่ได้ เพราะ Google Cloud Console ต้องล็อกอิน/คลิกยืนยันเอง):

1. ไปที่ [Google Cloud Console → Credentials](https://console.cloud.google.com/apis/credentials) สร้าง OAuth Client ID ประเภท **Web application** ตั้งค่า **Authorized JavaScript origins** เป็น `https://notezildjian-code.github.io` (และ `http://localhost:8080` ถ้าจะทดสอบในเครื่อง)
2. เอา Client ID ที่ได้ไปใส่ **2 ที่**:
   - ไฟล์ `js/admin-config.js` → `GOOGLE_CLIENT_ID`
   - `apps-script/Code.gs` → ตัวแปร `GOOGLE_CLIENT_ID`
   (ส่งมาให้ Claude ใส่ให้แล้ว redeploy ให้ก็ได้)
3. รายละเอียดทีละขั้นตอนเต็ม ๆ อยู่ใน `README.md` หัวข้อ "Admin page (`admin.html`)"

**หมายเหตุ**: ตอนแก้ `oauthScopes` ครั้งนี้ Google ไม่ได้ขึ้นหน้ายืนยันสิทธิ์ใหม่ให้เห็นทันที (ปกติควรมี) — คาดว่าจะขึ้นให้กดยืนยันครั้งแรกที่มีคนล็อกอินเข้า `admin.html` จริง ๆ แทน ถ้าเจอหน้านั้นตอนทดสอบ ต้องกดผ่านหน้า "unverified app" เอง (เป็น script ภายใน ไม่ต้องกังวล)

**สิทธิ์การเข้าถึงหน้า Admin**: จำกัดเฉพาะอีเมล `annop.p@sfg-th.com` เท่านั้น ตรวจสอบฝั่งเซิร์ฟเวอร์ใน `Code.gs` (`verifyAdminToken`) — ข้อมูลในชีตไม่ได้แชร์ให้บัญชีนี้โดยตรง ทุกการอ่าน/เขียนทำผ่านสิทธิ์ของตัว Apps Script เอง เพื่อความปลอดภัยของข้อมูลผู้สมัคร (เลขบัตรประชาชน, ข้อมูลสุขภาพ)

## ฟีเจอร์หน้า Admin ที่มี

- **จัดการตำแหน่งงาน**: เพิ่ม/แก้ไขชื่อ/เปิด-ปิดรับสมัคร
- **ดูรายชื่อผู้สมัคร**: ค้นหา/กรอง + คลิกดูรายละเอียดเต็มของผู้สมัครแต่ละคน
- **Export to Excel**: ปุ่มเดียวโหลดไฟล์ .xlsx ทั้งไฟล์ (ทุกแท็บ)
